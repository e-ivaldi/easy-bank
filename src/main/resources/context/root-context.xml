<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="http://www.springframework.org/schema/beans"
    xmlns:context="http://www.springframework.org/schema/context"	
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
    					http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd" >

    <!-- root Context: defines shared resources visible to all other web components -->
  	
  	
  	<!-- scan for all the annotations excluding the controllers -->
  	<context:component-scan base-package="com.manuv" use-default-filters="true">
	    <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
	</context:component-scan>	
	
	<!-- this bean managers the retrieving of the i18n strings -->
	<bean id="messageSource" class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
		<property name="basenames">  
	        <list>
	            <value>WEB-INF/messages/messages</value> 	            
	        </list>  
		</property>
		<property name="defaultEncoding" value="UTF-8"/>		
    </bean>
    
    <!-- handle the properties loading -->
	<bean id="propertyConfigurer" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<!-- by default this property is already true, this is just a reminder -->
		<property name="searchSystemEnvironment" value="true"/>
		<property name="location" value="classpath:/conf.properties" />
	</bean> 
	
	<import resource="db-context.xml"/>    
    <import resource="security-context.xml"/>

</beans>